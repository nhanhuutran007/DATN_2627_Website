[project]
name = "crowdfunding-ai-service"
version = "0.1.0"
requires-python = ">=3.12,<3.15"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
