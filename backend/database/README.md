# Persistence boundary

Đặt migration của backend tại `database/migrations/`. Chỉ chọn ORM/migration tool sau khi ERD và transaction ledger được duyệt; không tạo entity tạm rồi phụ thuộc vào auto-sync.
