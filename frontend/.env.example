NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
# Chỉ dùng phía server (SSR). Cần khi NEXT_PUBLIC_API_URL là đường dẫn tương đối
# (vd. /api/v1 sau Nginx); trong Docker Compose là http://backend:4000/api/v1.
# API_INTERNAL_URL=
